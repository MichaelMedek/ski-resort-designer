# Streamlit server config for HF Spaces.

[server]
headless = true
# HF serves the app inside an *.hf.space iframe; XSRF/CORS must be relaxed.
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
