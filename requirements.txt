# Requirements for ski_terrain_planner module
# Install with: pip install -r requirements.txt

# Web UI
streamlit>=1.30.0
streamlit-deckgl>=0.5.0  # Full deck.gl events including terrain clicks
streamlit-js-eval>=0.1.7  # Read parent.innerHeight so the map fills the viewport

# Geospatial processing
shapely>=2.0.0
pyproj>=3.6.0
rasterio>=1.3.0

# HTTP requests (for OSM Overpass API)
requests>=2.28.0

# Note: Download EuroDEM (2.3GB) from https://www.mapsforeurope.org/datasets/euro-dem
# Here a crop of the Alps is provided for convenience, but you can also use the full dataset

# Visualization - Pydeck for GPU-accelerated maps, Plotly for charts
pydeck>=0.8.0
plotly>=5.18.0
Pillow>=10.0.0  # PIL image rendering for OSM graph plots

# Data handling for Pydeck layers
pandas>=2.0.0

# Numerical
numpy>=1.24.0
scipy

# Graph algorithms — scipy.sparse.csgraph (shortest-path/SCC) + networkx (approximate ATSP for the
# scenic visit-every-lift route tours). networkx>=3.3 (osmnx also requires 3.x).
networkx>=3.3

# State machine
python-statemachine>=3.1.0

# Testing (dev)
pytest>=8.0.0
pytest-cov>=7.0.0
hypothesis>=6.0.0
mypy
pytest-mypy
pre-commit

# Type stubs for mypy
types-requests
types-shapely

# Hugging Face (Only to push the DEM to Hugging Face, not required for end users)
# huggingface-hub

# Editable install
-e .
